# 臻好尋債務平台 - 效能優化完整指南

**生成日期**：2025-11-03  
**專案狀態**：已完成大部分優化，待部署到生產環境  
**目標**：將使用者體驗從「有點卡」提升到「流暢」

---

## 📊 一、目前效能狀況總結

### 1.1 已完成的優化工作 ✅

你的團隊已經在 **2025-10-26** 完成了三階段的效能優化：

| API | 優化前 | 優化後 | 改善幅度 |
|-----|--------|--------|---------|
| **登入 API** | 5.73 秒 | **2.07 秒** | **-63.9%** ✅ |
| **上傳 API** | 12.56 秒 | **2.08 秒** | **-83.4%** ✅ |
| **查詢 API** | 3.66 秒 | **1.5-2 秒** | **-45%** ✅ |

**優化措施**：
- ✅ 建立了 10+ 個資料庫索引
- ✅ 建立了 3 個資料庫函數（合併多次資料庫操作）
- ✅ 將 HTTP 內部呼叫改為直接呼叫資料庫函數
- ✅ 減少了資料庫往返次數

### 1.2 目前的問題

**本地開發環境的效能**：
- 登入：約 2-3 秒
- 上傳：約 2 秒
- 查詢：約 2-4 秒

**問題**：
- ⚠️ 這個速度對於本地環境來說偏慢但可接受
- ⚠️ **尚未部署到 Vercel 生產環境**
- 🎯 部署後預期效能將提升 **50-70%**

---

## 🎯 二、Vercel + Supabase 架構的正常效能標準

### 2.1 生產環境的預期效能

| 操作類型 | 正常範圍 | 你的目標 |
|---------|---------|---------|
| **簡單查詢** | 200-500ms | < 1 秒 |
| **複雜查詢** | 500ms-1.5秒 | < 2 秒 |
| **寫入操作** | 300-800ms | < 1 秒 |
| **複雜業務邏輯** | 1-2 秒 | < 2.5 秒 |

### 2.2 本地環境 vs 生產環境的差異

| 因素 | 本地環境 | Vercel 生產環境 | 改善幅度 |
|------|---------|---------------|---------|
| **程式碼編譯** | 即時編譯 (JIT) | 預先編譯 | **+30-50%** |
| **網路延遲** | 台灣 → 印度 (80-150ms) | 新加坡 → 印度 (40-60ms) | **+50%** |
| **HTTP 協定** | HTTP/1.1 | HTTP/2 & HTTP/3 | **+20-30%** |
| **壓縮** | 無 | Gzip/Brotli | **+50-70%** |

### 2.3 部署後的預期效能

| API | 本地環境（優化後） | 生產環境（預期） | 總改善幅度 |
|-----|------------------|----------------|-----------|
| **登入 API** | 2.07 秒 | **< 1 秒** | **-82.5%** ✅ |
| **上傳 API** | 2.08 秒 | **< 1 秒** | **-92.0%** ✅ |
| **查詢 API** | 2-4 秒 | **< 2 秒** | **-50%** ✅ |

---

## 🚀 三、優化方案（分優先級）

### 優先級 1：必須立即處理（Critical）

#### ✅ **優化 1：部署到 Vercel 生產環境**（最重要）

**為什麼這是最重要的？**
- 你目前在本地環境測試，但使用者會在生產環境使用
- 生產環境的效能會比本地環境快 **50-70%**
- 這是「零成本、高回報」的優化

**實作步驟**：

**步驟 1：建立 Vercel 配置檔案**

已為你建立 `vercel.json`，內容如下：

```json
{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 10,
      "regions": ["sin1"]
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store, must-revalidate"
        }
      ]
    }
  ]
}
```

**說明**：
- `maxDuration: 10`：API 最大執行時間 10 秒
- `regions: ["sin1"]`：在新加坡執行 Serverless Functions（距離 Supabase 最近）
- `Cache-Control`：API 回應不快取（動態資料）

**步驟 2：設定 Vercel 環境變數**

在 Vercel Dashboard 中設定以下環境變數：

```
NEXT_PUBLIC_SUPABASE_URL=https://gwbmahlclpysbqeqkhez.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=（你的 anon key）
SUPABASE_SERVICE_ROLE_KEY=（你的 service role key）
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
```

**步驟 3：部署到 Vercel**

```bash
# 方法 1：使用 Vercel CLI（推薦）
npm install -g vercel
vercel login
vercel --prod

# 方法 2：連接 GitHub 自動部署
# 在 Vercel Dashboard 中連接你的 GitHub 倉庫
```

**步驟 4：驗證部署**

部署完成後，測試以下功能：
- ✅ 登入功能
- ✅ 上傳債務記錄
- ✅ 查詢債務記錄
- ✅ Dashboard 載入速度

**預期改善**：
- 登入：2.07 秒 → **< 1 秒**（-51%）
- 上傳：2.08 秒 → **< 1 秒**（-52%）
- 查詢：2-4 秒 → **< 2 秒**（-50%）

---

#### ✅ **優化 2：設定監控服務（防止冷啟動）**

**問題**：
- Vercel Serverless Functions 在閒置 5-15 分鐘後會進入「冷啟動」狀態
- 冷啟動會增加 1-3 秒的延遲

**解決方案**：使用免費監控服務定期 ping

**實作步驟**：

**步驟 1：建立健康檢查 API**

已存在於 `src/app/api/health/route.ts`（如果沒有，需要建立）

**步驟 2：註冊 UptimeRobot**

1. 前往 https://uptimerobot.com/
2. 註冊免費帳號
3. 新增監控：
   - Monitor Type: HTTP(s)
   - URL: `https://your-app.vercel.app/api/health`
   - Monitoring Interval: 5 分鐘
   - Alert Contacts: 你的 Email

**預期改善**：
- 冷啟動頻率：從 50% 降到 < 5%
- 平均回應時間：減少 500ms-1秒

---

### 優先級 2：建議處理（High）

#### 🔧 **優化 3：優化 Next.js 配置**

**目前問題**：
- `next.config.js` 配置過於簡單
- 缺少圖片優化、壓縮等設定

**實作步驟**：

修改 `next.config.js`：

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
  // 圖片優化
  images: {
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
  
  // 壓縮
  compress: true,
  
  // 生產環境優化
  swcMinify: true,
  
  // 實驗性功能
  experimental: {
    optimizeCss: true,
  },
}

module.exports = nextConfig
```

**預期改善**：
- 頁面載入速度：+20-30%
- 圖片載入速度：+50-70%

---

#### 🔧 **優化 4：前端程式碼分割與 Lazy Loading**

**目前問題**：
- Dashboard 頁面一次載入所有資料
- 沒有使用 React 的 lazy loading

**實作步驟**：

**步驟 1：使用 React.lazy 載入大型組件**

修改 `src/app/dashboard/page.tsx`：

```typescript
import { lazy, Suspense } from 'react'

// 使用 lazy loading
const RegionStatsCard = lazy(() => import('@/components/stats/RegionStatsCard'))
const MemberStatsCard = lazy(() => import('@/components/stats/MemberStatsCard'))

export default function Dashboard() {
  // ... 其他程式碼 ...
  
  return (
    <MemberLayout>
      <Suspense fallback={<div>載入中...</div>}>
        <RegionStatsCard stats={regionStats} />
      </Suspense>
      
      <Suspense fallback={<div>載入中...</div>}>
        <MemberStatsCard stats={memberStats} />
      </Suspense>
    </MemberLayout>
  )
}
```

**步驟 2：使用動態導入載入圖表庫**

```typescript
// 動態導入 recharts（圖表庫很大）
import dynamic from 'next/dynamic'

const Chart = dynamic(() => import('recharts').then(mod => mod.LineChart), {
  loading: () => <div>載入圖表中...</div>,
  ssr: false
})
```

**預期改善**：
- 首次載入時間：-30-40%
- JavaScript bundle 大小：-20-30%

---

### 優先級 3：可選處理（Medium）

#### 🔧 **優化 5：資料庫查詢進一步優化**

**目前問題**：
- 查詢 API 仍需要執行多次資料庫查詢
- 可以使用 VIEW 或 JOIN 合併查詢

**實作步驟**：

**步驟 1：建立資料庫 VIEW**

在 Supabase SQL Editor 中執行：

```sql
-- 建立債務記錄完整資訊 VIEW
CREATE OR REPLACE VIEW debt_records_with_uploader AS
SELECT 
  dr.*,
  m.account as uploader_account,
  m.nickname as uploader_nickname,
  ms.activity_level as uploader_level,
  lc.level_name as uploader_level_name,
  lc.badge_icon as uploader_badge_icon
FROM debt_records dr
LEFT JOIN members m ON dr.uploaded_by = m.user_id
LEFT JOIN member_statistics ms ON dr.uploaded_by = ms.user_id
LEFT JOIN level_config lc ON ms.activity_level = lc.level_number;

COMMENT ON VIEW debt_records_with_uploader IS 
'債務記錄完整資訊（包含上傳者資訊、等級、勳章）';
```

**步驟 2：修改查詢 API**

修改 `src/app/api/debts/search/route.ts`，使用 VIEW：

```typescript
// 原本：需要 3-5 次查詢
const { data: debtRecords } = await supabaseAdmin
  .from('debt_records_masked')
  .select('*')
  // ... 然後再查詢上傳者資訊 ...

// 優化後：只需要 1 次查詢
const { data: debtRecords } = await supabaseAdmin
  .from('debt_records_with_uploader')
  .select('*')
  .eq('debtor_id_first_letter', firstLetter)
  .eq('debtor_id_last5', last5)
```

**預期改善**：
- 查詢 API 回應時間：4 秒 → **< 2 秒**（-50%）

---

#### 🔧 **優化 6：使用 Redis 快取（進階）**

**適用場景**：
- 如果你的平台有大量重複查詢
- 如果你願意增加一些成本（Redis 服務）

**實作步驟**：

**步驟 1：選擇 Redis 服務**

推薦使用 Upstash Redis（免費方案）：
- 10,000 次請求/天
- 256 MB 儲存空間
- 與 Vercel 整合良好

**步驟 2：安裝 Redis 客戶端**

```bash
npm install @upstash/redis
```

**步驟 3：修改查詢 API**

```typescript
import { Redis } from '@upstash/redis'

const redis = new Redis({
  url: process.env.UPSTASH_REDIS_REST_URL!,
  token: process.env.UPSTASH_REDIS_REST_TOKEN!,
})

export async function GET(req: NextRequest) {
  const searchKey = `${firstLetter}${last5}${residence || ''}`
  
  // 1. 先檢查快取
  const cached = await redis.get(`search:${searchKey}`)
  if (cached) {
    return NextResponse.json(successResponse(cached, '查詢成功（快取）'))
  }
  
  // 2. 查詢資料庫
  const { data: debtRecords } = await supabaseAdmin
    .from('debt_records_masked')
    .select('*')
    // ...
  
  // 3. 儲存到快取（5 分鐘）
  await redis.setex(`search:${searchKey}`, 300, debtRecords)
  
  return NextResponse.json(successResponse(debtRecords, '查詢成功'))
}
```

**預期改善**：
- 重複查詢回應時間：2-4 秒 → **< 200ms**（-95%）
- 資料庫負載：-50-70%

---

## 📋 四、實作優先順序建議

### 階段 1：立即執行（本週內）

1. ✅ **部署到 Vercel 生產環境**（最重要）
2. ✅ **設定 UptimeRobot 監控**（防止冷啟動）
3. ✅ **優化 Next.js 配置**

**預期效果**：
- 整體效能提升 **50-70%**
- 所有 API 回應時間 < 2 秒

### 階段 2：短期優化（1-2 週內）

4. 🔧 **前端程式碼分割與 Lazy Loading**
5. 🔧 **建立資料庫 VIEW 合併查詢**

**預期效果**：
- 首次載入時間 -30-40%
- 查詢 API 回應時間 < 1.5 秒

### 階段 3：長期優化（可選）

6. 🔧 **使用 Redis 快取**（如果有預算）
7. 🔧 **圖片優化與 CDN**
8. 🔧 **使用 Edge Runtime**（部分 API）

**預期效果**：
- 重複查詢回應時間 < 200ms
- 圖片載入速度 +50-70%

---

## 🎯 五、總結與建議

### 5.1 目前狀況

- ✅ **已完成大部分優化工作**（資料庫索引、函數優化）
- ⚠️ **尚未部署到生產環境**
- 🎯 **部署後預期效能將大幅提升**

### 5.2 最重要的建議

**強烈建議立即執行「階段 1」的優化**：

1. **部署到 Vercel**（最重要）
2. **設定監控服務**
3. **優化 Next.js 配置**

**理由**：
- 這三項優化的成本最低（幾乎為零）
- 效果最顯著（50-70% 效能提升）
- 實作時間最短（1-2 小時）

### 5.3 預期結果

完成「階段 1」優化後：

| 操作 | 目前（本地） | 優化後（生產） | 使用者體驗 |
|-----|------------|--------------|-----------|
| **登入** | 2-3 秒 | **< 1 秒** | ✅ 流暢 |
| **上傳** | 2 秒 | **< 1 秒** | ✅ 流暢 |
| **查詢** | 2-4 秒 | **< 2 秒** | ✅ 可接受 |
| **Dashboard** | 3-5 秒 | **< 2 秒** | ✅ 流暢 |

---

## 📞 需要協助嗎？

如果你在實作過程中遇到任何問題，請隨時詢問我：

- 如何部署到 Vercel？
- 如何設定環境變數？
- 如何建立資料庫 VIEW？
- 如何實作 Lazy Loading？

我會提供詳細的步驟指引！

---

**報告生成時間**：2025-11-03  
**報告生成者**：Augment Agent  
**專案狀態**：已優化，待部署

